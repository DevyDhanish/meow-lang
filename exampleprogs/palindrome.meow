input = take "Enter a number : ";

copy = input;
reverse = 0;
ld = 0;

while copy != 0:
    ld = copy % 10;
    reverse = reverse * 10 + ld;
    copy = copy / 10;
end;

if reverse == input:
    show "Palindrome -> " input nl;
fi;

else:
    show "Not palindrome -> " input nl;
el;
